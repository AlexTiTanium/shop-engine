<?php

{% for path in use %}
use {{ path }};
{% endfor %}

$router = Core::getController('{{ controller }}')->getRouter()->logged();

// Default
#$router->addEventListener('DefaultListener')->When(array('type'=>'json'));